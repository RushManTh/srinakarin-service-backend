# Copy this file to .env and adjust values as needed

# App
NODE_ENV=production
JWT_SECRET=change_me_to_strong_secret_key

# Database (external Postgres example)
# Note: Provide your own external DB connection string
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# Optional: Custom port mapping (default: 3001)
# APP_PORT=3001
